# LofiGirl Terminal - Environment Configuration Example
# Copy this file to .env and customize the values

# Application Settings
APP_NAME=lofigirl-terminal
APP_VERSION=0.1.0
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# Audio Settings
DEFAULT_VOLUME=50  # 0-100
AUDIO_QUALITY=high  # low, medium, high
AUDIO_CACHE_ENABLED=false
AUDIO_CACHE_DIR=.cache/audio

# Network Settings
CONNECTION_TIMEOUT=30  # seconds
RETRY_ATTEMPTS=3
STREAM_BUFFER_SIZE=4096

# UI Settings
THEME=default  # default, dark, light
SHOW_VISUALIZER=true
UPDATE_INTERVAL=1  # seconds

# Default Station
DEFAULT_STATION=lofi-hip-hop

# Development Settings (only for development)
DEBUG_MODE=false
ENABLE_PROFILING=false

# API Keys

# YouTube Data API v3 Key (Optional but recommended)
# Used to scan @LofiGirl channel for live streams automatically
# Without this key, the app will use RSS feed or web scraping (less reliable)
#
# To get an API key:
# 1. Go to https://console.developers.google.com/
# 2. Create a new project or select existing one
# 3. Enable "YouTube Data API v3"
# 4. Create credentials (API key)
# 5. Paste the key below and uncomment the line
#
# YOUTUBE_API_KEY=your_api_key_here

# SPOTIFY_CLIENT_ID=your_client_id_here  # Future feature
